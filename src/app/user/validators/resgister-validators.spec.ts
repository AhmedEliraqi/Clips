import { ResgisterValidators } from './resgister-validators';

describe('ResgisterValidators', () => {
  it('should create an instance', () => {
    expect(new ResgisterValidators()).toBeTruthy();
  });
});
